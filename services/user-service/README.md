# User Service (scaffold)

Minimal scaffold for the user microservice.

How to run locally:

```bash
cd backend/services/user-service
npm install
npm run dev
```

This service will later verify Firebase ID tokens (using `firebase-admin`) and persist user profiles to MongoDB.
